{
  "name": "@cerniq/cf-verify",
  "version": "0.1.0",
  "private": true,
  "description": "CERNIQ Cloudflare Worker — global edge port of /v1/verify (Phase 3).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "echo 'Phase 3 only — gated behind $5K CERNIQ MRR. Edit me when ready.' && exit 1",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings=0"
  },
  "dependencies": {
    "@cerniq/types": "workspace:*",
    "@noble/ed25519": "^2.1.0",
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@cerniq/tsconfig": "workspace:*",
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.95.0"
  }
}
