{
  "name": "@cerniq-examples/node-quickstart",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "60-line CERNIQ quickstart — principal → agent → policy → sign → verify.",
  "scripts": {
    "start": "tsx src/quickstart.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cerniq/sdk": "workspace:*"
  },
  "devDependencies": {
    "@cerniq/tsconfig": "workspace:*",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
