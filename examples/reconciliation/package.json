{
  "name": "@okoro-examples/reconciliation",
  "private": true,
  "version": "0.1.0",
  "description": "Joins OKORO audit events to underlying-system records, surfaces the four mismatch classes from INTEGRATION_PATTERNS §10.",
  "type": "module",
  "scripts": {
    "demo": "tsx src/cli.ts --okoro fixtures/okoro-export.ndjson --psp fixtures/psp-charges.ndjson",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@okoro/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
