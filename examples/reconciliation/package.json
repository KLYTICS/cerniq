{
  "name": "@cerniq-examples/reconciliation",
  "private": true,
  "version": "0.1.0",
  "description": "Joins CERNIQ audit events to underlying-system records, surfaces the four mismatch classes from INTEGRATION_PATTERNS §10.",
  "type": "module",
  "scripts": {
    "demo": "tsx src/cli.ts --cerniq fixtures/cerniq-export.ndjson --psp fixtures/psp-charges.ndjson",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cerniq/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
