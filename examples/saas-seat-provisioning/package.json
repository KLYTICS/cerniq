{
  "name": "@cerniq-examples/saas-seat-provisioning",
  "private": true,
  "version": "0.1.0",
  "description": "SCIM-flavored agent provisioning + per-seat policies",
  "type": "module",
  "scripts": {
    "dev": "tsx src/scim-server.ts",
    "batch": "tsx src/provisioning-batch.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cerniq/sdk": "workspace:*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@cerniq/tsconfig": "workspace:*",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
