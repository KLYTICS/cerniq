{
  "name": "@aegis-tools/quickstart",
  "private": true,
  "version": "0.1.0",
  "description": "Your first AEGIS verify in 30 seconds — registers an agent, signs a token, calls verify, prints the verdict.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aegis/sdk": "workspace:*"
  },
  "devDependencies": {
    "@aegis/tsconfig": "link:../../packages/tsconfig",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
