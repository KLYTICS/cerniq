---
title: Agents API
description: Register, list, revoke, and inspect agent identities. Authentication via X-OKORO-API-Key with FULL scope.
---

The full OpenAPI spec lives at
[`docs/spec/OKORO_API_SPEC.yaml`](https://github.com/klytics/okoro/blob/main/docs/spec/OKORO_API_SPEC.yaml).
A `fumadocs-openapi`-generated, try-it-out-capable reference is the next
content drop on this route (`pnpm --filter @okoro/docs openapi:generate`).

## Endpoints in this slice

| Method | Path                       | Purpose                                   |
| ------ | -------------------------- | ----------------------------------------- |
| POST   | `/v1/agents`               | Register a new agent (public key required) |
| GET    | `/v1/agents`               | List agents owned by the calling principal |
| GET    | `/v1/agents/:id`           | Inspect a single agent                    |
| DELETE | `/v1/agents/:id`           | Revoke an agent (irreversible)            |

Authentication: `X-OKORO-API-Key` header with **FULL** scope. A
`X-OKORO-Verify-Key` (verify-only) is rejected by this namespace.

## Example — register

```bash
curl -X POST https://api.okoroapp.com/v1/agents \
  -H "X-OKORO-API-Key: $OKORO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "publicKey": "MCowBQYDK2VwAyEA...",
    "label": "checkout-bot",
    "metadata": { "env": "production" }
  }'
```

Response (`201 Created`):

```json
{
  "id": "agent_01HXYZ...",
  "principalId": "prn_01HXYZ...",
  "label": "checkout-bot",
  "publicKey": "MCowBQYDK2VwAyEA...",
  "trustScore": 500,
  "trustBand": "VERIFIED",
  "createdAt": "2026-05-08T14:32:11Z",
  "revokedAt": null
}
```

## Failure modes

Every error returns the standard OKORO error envelope:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "publicKey must be a valid Ed25519 SPKI",
    "requestId": "req_01HXYZ..."
  }
}
```

The full error catalog is in `packages/types/src/error-catalog.generated.ts`.
