---
title: Developer
description: Build with CERNIQ. SDKs, quickstarts, MCP integration, recipes.
---

If you're integrating an AI agent with a service that performs real-world
actions — charging cards, provisioning seats, calling APIs, writing files —
CERNIQ is the verification layer between the two.

<SdkVersionBadges />

## Five-minute path

1. **Install** the SDK for your language — see badges above.
2. **Mint a keypair** with `generateKeypair()`. Private key stays in your process.
3. **Register the agent** via `cerniq.agents.register({ publicKey })`. CERNIQ only ever sees the public key.
4. **Issue a scoped policy** with `cerniq.policies.create({ scope, ttlSeconds })`.
5. **Verify before acting** with `await cerniq.verify(token)` — if `allowed === false`, do not perform the action.

Full walkthroughs:

- [TypeScript Quickstart](/docs/quickstart/typescript)
- [Fintech: payments authorization](/docs/quickstart/fintech-payments)
- [AI platforms: MCP tool calls](/docs/quickstart/ai-platform-tool-call)
- [SaaS: SCIM seat provisioning](/docs/quickstart/saas-seat-provisioning)

## SDK surface

| Method                          | Purpose                                  |
| ------------------------------- | ---------------------------------------- |
| `cerniq.agents.register(input)`  | Register a new agent public key          |
| `cerniq.agents.list()`           | List agents owned by this principal      |
| `cerniq.agents.show(id)`         | Inspect a single agent                   |
| `cerniq.agents.revoke(id)`       | Revoke an agent (irreversible)           |
| `cerniq.policies.create(input)`  | Issue a scoped, signed policy            |
| `cerniq.policies.list()`         | List active policies                     |
| `cerniq.policies.revoke(id)`     | Revoke a policy                          |
| `cerniq.verify(token)`           | The verify hot path                      |
| `cerniq.audit.list(params)`      | Paginated audit-event reader             |
| `cerniq.audit.export(params)`    | NDJSON streaming export for SOC2 evidence |

Both TypeScript and Python SDKs preserve byte-identical JWT shapes — see
the [denial precedence](/docs/concepts/denial-precedence) page for the wire
contract relying parties build retry logic on.

## Recipes

| Need                                                   | Read                                                                                  |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------- |
| Verify offline (no network round-trip in the hot path) | [`@cerniq/verifier-rp`](https://github.com/klytics/cerniq/tree/main/packages/verifier-rp) |
| Subscribe to lifecycle events                          | [Webhooks concept](/docs/concepts/webhooks)                                           |
| Integrate with Claude Desktop / MCP                    | [`@cerniq/mcp-server`](https://github.com/klytics/cerniq/tree/main/packages/mcp-server) |
| Add CERNIQ to a LangChain agent                         | [`examples/ai-platform-tool-call`](https://github.com/klytics/cerniq/tree/main/examples/ai-platform-tool-call) |
