// Vitest stub for the Next.js `server-only` package. In production it
// throws at module-eval if imported from a client bundle; in node test
// context it's a no-op so server-flagged modules can be unit-tested.
export {};
